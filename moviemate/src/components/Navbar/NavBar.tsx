import React from "react";

export const NavBar: React.FC = () => {
    return (
        <nav>
        <h1>Moviemate</h1>
        <div>
            <button>Log in</button>
            <button>Sign up</button>
        </div>
        </nav>
    );
    }